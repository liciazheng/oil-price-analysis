# Pinned to the versions this analysis was last verified against.
#
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#
# Verified 2026-09-11 on CPython 3.13.15 (Windows 11).
#
# Note on prophet: the package installs from a prebuilt wheel, but its Stan
# backend fails to start on this machine (exit code 0xC0000139, a missing DLL
# entry point) — on Python 3.11 as well, so it is not a version issue. The
# analysis is built around the ARIMA forecast; the Prophet overlay is guarded
# by try/except and simply does not appear. See the Limitations section of the
# README.

# Direct dependencies
pandas==3.0.5
numpy==2.5.3
matplotlib==3.11.2
statsmodels==0.15.0
yfinance==1.7.0
prophet==1.4.0

# Transitive, pinned so the environment is reproducible
beautifulsoup4==4.15.0
certifi==2026.7.22
cffi==2.1.1
charset-normalizer==3.5.1
cmdstanpy==1.3.0
colorama==0.4.6
contourpy==1.4.0
curl_cffi==0.16.3
cycler==0.12.1
fonttools==4.65.0
formulaic==1.2.2
holidays==0.104
idna==3.19
interface_meta==2.0.1
kiwisolver==1.5.1
lxml==6.1.3
multitasking==0.0.13
narwhals==2.26.0
packaging==26.3
patsy==1.0.3
peewee==4.5.1
pillow==12.3.0
platformdirs==4.11.8
protobuf==7.36.1
pycparser==3.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
pytz==2026.3.post1
requests==2.34.2
scipy==1.18.1
six==1.17.0
soupsieve==2.9.2
stanio==0.5.1
tqdm==4.70.1
typing_extensions==4.16.0
tzdata==2026.3
urllib3==2.7.0
websockets==17.1
wrapt==2.4.1
